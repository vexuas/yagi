{
  "name": "yagi",
  "version": "3.1.0",
  "description": "Vulture's Vale/Blizzard Berg World Boss Timer for Aura Kingdom",
  "main": "yagi.js",
  "author": "Vexuas",
  "scripts": {
    "test": "mocha",
    "start": "yarn prebuild && yarn build && yarn nodemon dist/yagi.js",
    "start:prod": "yarn prebuild && yarn build && BOT_ENV=prod yarn nodemon dist/yagi.js",
    "prebuild": "node -p \"'export const BOT_VERSION = ' + JSON.stringify(require('./package.json').version) + ';'\" > src/version.ts",
    "build": "tsc",
    "auto:changelog": "yarn auto changelog",
    "auto:version": "yarn version --`auto version`",
    "auto:release": "yarn auto:changelog && yarn auto:version",
    "release": "git fetch --tags --force && git checkout -b release/$(date +'%Y-%m-%d') && yarn auto:release && git push -u origin head"
  },
  "dependencies": {
    "@discordjs/rest": "^1.0.1",
    "@top-gg/sdk": "^3.1.2",
    "chai": "^4.2.0",
    "date-fns": "^1.30.1",
    "discord-api-types": "^0.37.0",
    "discord.js": "^13.3.1",
    "googleapis": "39",
    "lodash": "^4.17.21",
    "mixpanel": "^0.13.0",
    "mocha": "^6.2.0",
    "nodemon": "^2.0.7",
    "pg": "^8.9.0",
    "topgg-autoposter": "^2.0.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.191",
    "@types/pg": "^8.6.6",
    "auto": "^10.13.1",
    "eslint": "^8.35.0",
    "eslint-plugin-unused-imports": "^2.0.0",
    "typescript": "^4.9.5"
  }
}
